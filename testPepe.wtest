import pepe.*

describe "Sueldo Pepe" {
	
	test "Test 1" {
		pepe.diasQueFalto(3)
		assert.equals(pepe.sueldo(), 20800)
	}
	
}

describe "bonus" {
  test "sueldo Pepe" {
	medioTiempo.categoriaBase(cadete)
	pepe.categoria(medioTiempo)
	pepe.tipoDeBonoPorResultado(bonoPorcentaje)
	pepe.tipoDeBonoPorPresentismo(bonoDemagogico)
	pepe.diasQueFalto(1)
	assert.equals(pepe.sueldo(), 11500)
  }
}